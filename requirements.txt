# Minimal, CPU-friendly dependencies for repo setup and CI smoke tests.
# The full runtime environments are defined in linux-runtime-pins.txt and
# windows-runtime-pins.txt and installed by the bootstrap scripts.

joblib>=1.4,<2
numpy<2
opencv-python-headless>=4.10,<5
pandas>=2.2,<3
torch==2.8.0
torchaudio==2.5.1
torchvision==0.20.1
tqdm>=4.66,<5
ultralytics>=8.3,<9
